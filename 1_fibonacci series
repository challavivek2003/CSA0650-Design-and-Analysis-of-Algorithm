#include<stdio.h>
int main()
{
	printf("Vivek-192211834\n");
 	int a=0,b=1,c,i,n;
 	printf("entre the number:");
 	scanf("%d",&n);
 	printf("%d",a);
 	printf("%d",b);
 	for(i=2;i<n;i++)
 	{
 		c=a+b;
 		printf("%d",c);
 		a=b;
 		b=c;
	 }
	 return 0;
}
